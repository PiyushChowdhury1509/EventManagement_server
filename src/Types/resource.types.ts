export enum ResourceType {
  EVENT = "event",
  NOTICE = "notice",
  COMMENT = "comment",
}

export enum LikeType {
  LIKE = "1",
  UNLIKE = "0",
}

export enum NoticeStatusType {
  EXPIRED = "expired",
  UPCOMING = "upcoming",
  URGENT = "urgent",
}

export enum PostType {
  EVENT = "event",
  NOTICE = "notice",
}
